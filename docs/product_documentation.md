# This document is deprecated.

This document described the old GUI-based application workflow, which has been entirely replaced by the new web-based interface.

It needs to be rewritten to reflect the new architecture and user experience.
