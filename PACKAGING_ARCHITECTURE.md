# 打包流程架构图

## 📐 整体打包架构

```
┌─────────────────────────────────────────────────────────────────────┐
│                   ZR Daily Report 打包系统                           │
└─────────────────────────────────────────────────────────────────────┘

                            源代码 (分支: development-copy)
                                      │
                    ┌─────────────────┴─────────────────┐
                    ↓                                   ↓
          ┌──────────────────┐           ┌────────────────────┐
          │   方式1: PyPI    │           │  方式2: Windows    │
          │   标准Python包    │           │  冻结发行版        │
          └──────────────────┘           └────────────────────┘
                    │                             │
                    ↓                             ↓
          python -m build              python build_package.py frozen
                    │                             │
          ┌─────────┴─────────┐         ┌────────┴───────┐
          ↓                   ↓         ↓                ↓
       .whl               .tar.gz    install_report.bat
                                     run_report.bat
                                     requirements.txt
                                     README.txt
                                     zr_daily_report/
                    │                             │
                    ↓                             ↓
              上传到PyPI              打包为ZIP文件
                    │                             │
              pip install          zr_daily_report_
              zr-daily-report      v1.0.0.zip
```

---

## 🔄 方式1: PyPI标准包 详细流程

```
┌─────────────────────────────────────────────────────────────────┐
│ 源代码 (development-copy)                                        │
│ ├── src/                                                         │
│ ├── tests/                                                       │
│ ├── pyproject.toml         ← 版本信息、依赖定义                │
│ ├── setup.py               ← 构建脚本                           │
│ └── zr_daily_report.py                                           │
└─────────────────────────────────────────────────────────────────┘
           │
           │ python -m build
           ↓
┌─────────────────────────────────────────────────────────────────┐
│ 构建系统 (setuptools)                                            │
│ 1. 解析 pyproject.toml                                           │
│ 2. 编译 Python代码                                               │
│ 3. 生成元数据                                                     │
│ 4. 打包为wheel和sdist格式                                        │
└─────────────────────────────────────────────────────────────────┘
           │
           │ 输出结果
           ↓
┌─────────────────────────────────────────────────────────────────┐
│ dist/                                                             │
│ ├── zr_daily_report-1.0.0-py3-none-any.whl   (~2MB)            │
│ │   ├── zr_daily_report/                                         │
│ │   ├── zr_daily_report-1.0.0.dist-info/                        │
│ │   └── ...                                                       │
│ │                                                                 │
│ └── zr_daily_report-1.0.0.tar.gz              (~3MB)            │
│     ├── zr-daily-report-1.0.0/                                  │
│     ├── PKG-INFO                                                 │
│     └── ...                                                       │
└─────────────────────────────────────────────────────────────────┘
           │
           │ twine upload dist/*
           │ (需要PyPI API Token)
           ↓
┌─────────────────────────────────────────────────────────────────┐
│ PyPI 官方仓库                                                     │
│ https://pypi.org/project/zr-daily-report/1.0.0/                 │
└─────────────────────────────────────────────────────────────────┘
           │
           │ pip install zr-daily-report
           ↓
┌─────────────────────────────────────────────────────────────────┐
│ 用户 Python 环境                                                  │
│ ~/.local/lib/python3.10/site-packages/                           │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🎯 方式2: Windows冻结版本 详细流程

```
┌──────────────────────────────────────────────────────────────────────┐
│ 源代码 (development-copy)                                             │
│ ├── src/                                                              │
│ ├── config/                 ← 数据库配置                             │
│ ├── template/               ← Excel模板                              │
│ ├── test_data/                                                        │
│ ├── zr_daily_report.py      ← 主程序入口                             │
│ ├── pyproject.toml                                                    │
│ ├── requirements.txt         ← 完整依赖列表                          │
│ └── build_package.py        ← 打包脚本                               │
└──────────────────────────────────────────────────────────────────────┘
                                    │
                        python build_package.py frozen
                                    │
            ┌───────────────────────┼───────────────────────┐
            ↓                       ↓                       ↓
    ┌──────────────┐       ┌───────────────┐       ┌──────────────┐
    │ 步骤1        │       │ 步骤2          │       │ 步骤3        │
    │ 创建目录结构 │       │ 复制项目文件   │       │ 生成批处理   │
    └──────────────┘       └───────────────┘       └──────────────┘
            │                       │                       │
            ↓                       ↓                       ↓
    frozen_dist/           复制到frozen_dist/      install_report.bat
    ├── zr_daily_report/   zr_daily_report/        run_report.bat
    │   ├── src/                                   README.txt
    │   ├── config/        ├── zr_daily_report.py
    │   ├── template/      ├── pyproject.toml
    │   └── ...            └── src/
    │
    └────────────────────────────────────────────────────────────┐
                                                                  │
                          ┌─────────────────────────────────────┘
                          │
                          ↓
                ┌─────────────────────┐
                │ 步骤4               │
                │ 生成requirements.txt│
                │ (核心依赖)          │
                └─────────────────────┘
                          │
                          ↓
                ┌─────────────────────────────────┐
                │ frozen_dist/requirements.txt     │
                │ openpyxl==3.1.0                │
                │ mysql-connector-python==8.0.33│
                └─────────────────────────────────┘
                          │
                          │ (所有文件准备完成)
                          ↓
                ┌─────────────────────┐
                │ 步骤5               │
                │ 创建ZIP压缩包        │
                └─────────────────────┘
                          │
                          ↓
            ┌──────────────────────────┐
            │ zr_daily_report_v1.0.0.zip
            │ (~15-20MB)
            │
            ├── install_report.bat
            ├── run_report.bat
            ├── README.txt
            ├── requirements.txt
            │
            └── zr_daily_report/
                ├── src/
                ├── config/
                ├── template/
                ├── zr_daily_report.py
                └── pyproject.toml
            └─────────────────────────────┘
                          │
                          │ 分发给Windows用户
                          ↓
            ┌──────────────────────────────┐
            │ 用户操作:                     │
            │ 1. 解压ZIP文件               │
            │ 2. 双击install_report.bat    │
            │    ├─ 检查Python环境        │
            │    ├─ 创建虚拟环境          │
            │    ├─ 安装依赖包            │
            │    └─ 安装程序本身          │
            │ 3. 双击run_report.bat        │
            │    ├─ 激活虚拟环境          │
            │    └─ 运行程序              │
            └──────────────────────────────┘
```

---

## 🔄 CI/CD 自动构建流程

```
┌────────────────────────────────────────────────────┐
│ 事件触发                                            │
│ - Push到master/development                         │
│ - Pull Request到master                             │
│ - 手动触发(workflow_dispatch)                       │
└────────────────────────────────────────────────────┘
                         │
                         ↓
        ┌────────────────┴────────────────┐
        ↓                                 ↓
    ┌───────────┐             ┌──────────────────┐
    │ Test Job  │             │ Code Quality Job │
    │ (并行)    │             │ (并行)           │
    └───────────┘             └──────────────────┘
        │                             │
        ├─ 检出代码                  ├─ Flake8检查
        ├─ 设置Python (3.8-3.11)    ├─ Black检查
        ├─ 安装依赖                  ├─ isort检查
        ├─ 自动生成测试               ├─ MyPy类型检查
        ├─ Flake8代码检查            └─ 覆盖率检查 (≥80%)
        ├─ Pytest单元测试
        └─ 上传覆盖率报告
                │                       │
                └───────────┬───────────┘
                            │
                    ┌───────↓───────┐
                    │               │
              测试通过?        代码质量?
              (全部YES)         (全部PASS)
                    └───────┬───────┘
                            │ (YES)
                            ↓
                ┌──────────────────────┐
                │ Docs Job (并行)       │
                │ - MkDocs生成文档      │
                │ - 上传文档制品        │
                └──────────────────────┘
                            │
                    ┌───────┴───────┐
                    │               │
                Test✓    Code✓    Docs✓
                    │               │
                    └───────┬───────┘
                            │ (全部通过)
                            ↓
                    ┌──────────────────┐
                    │ Build Job        │
                    │ - python -m build│
                    │ - 生成dist/      │
                    │ - 上传构件       │
                    └──────────────────┘
                            │
                ┌───────────┴───────────┐
                │                       │
            是否master分支?          否
            且是push事件?
                │ (是)                  │
                ↓                       ↓
        ┌─────────────────┐     ✅ 流程结束
        │ Deploy Job      │     (不上传PyPI)
        │ - 下载构件      │
        │ - 上传到PyPI    │
        │   (需要token)   │
        └─────────────────┘
                │
                ↓
        ✅ 流程结束 (自动发布到PyPI)
```

---

## 📋 install_report.bat 执行流程

```
┌────────────────────────────────────────────────────────┐
│ install_report.bat                                      │
│ (用户双击执行)                                         │
└────────────────────────────────────────────────────────┘
          │
          │ (批处理启动)
          ↓
┌────────────────────────────────────────────────────────┐
│ 第1步: 检查Python环境                                  │
│ - 运行 python --version                                │
│ - 检查返回码                                            │
│ - 如果失败 → 提示用户安装Python3.8+                    │
└────────────────────────────────────────────────────────┘
          │ (SUCCESS)
          ↓
┌────────────────────────────────────────────────────────┐
│ 第2步: 检查Python版本 (≥3.8)                          │
│ - 运行 python -c "import sys; ..."                     │
│ - 如果 < 3.8 → 提示版本不符                            │
└────────────────────────────────────────────────────────┘
          │ (SUCCESS)
          ↓
┌────────────────────────────────────────────────────────┐
│ 第3步: 创建虚拟环境                                    │
│ - 运行 python -m venv venv                             │
│ - 如果失败 → 提示磁盘空间或权限问题                    │
└────────────────────────────────────────────────────────┘
          │ (SUCCESS)
          ↓
┌────────────────────────────────────────────────────────┐
│ 第4步: 激活虚拈环境                                    │
│ - 运行 venv\Scripts\activate.bat                       │
│ - 如果失败 → 提示虚拈环境问题                          │
└────────────────────────────────────────────────────────┘
          │ (SUCCESS)
          ↓
┌────────────────────────────────────────────────────────┐
│ 第5步: 安装依赖包                                      │
│ - 运行 pip install -r requirements.txt                 │
│        -i https://mirrors.aliyun.com/pypi/simple/     │
│ - 安装: openpyxl, mysql-connector-python               │
│ - 如果失败 → 提示网络或镜像源问题                      │
└────────────────────────────────────────────────────────┘
          │ (SUCCESS)
          ↓
┌────────────────────────────────────────────────────────┐
│ 第6步: 安装程序                                        │
│ - 运行 pip install ./zr_daily_report                   │
│ - 如果失败 → 提示项目文件问题                          │
└────────────────────────────────────────────────────────┘
          │ (SUCCESS)
          ↓
┌────────────────────────────────────────────────────────┐
│ ✅ 安装完成!                                           │
│                                                         │
│ 下一步:                                                │
│ 双击 "run_report.bat" 启动程序                        │
│                                                         │
│ 按任意键退出窗口...                                   │
└────────────────────────────────────────────────────────┘
```

---

## 🔀 版本号更新流程

```
┌─────────────────────────────────────────┐
│ 当前状态                                 │
│ Git标签: v0.9                           │
│ 距离标签: 11个提交                       │
│ pyproject.toml: version = "1.0.0"       │
└─────────────────────────────────────────┘
          │
          │ (需要发布新版本)
          ↓
┌─────────────────────────────────────────┐
│ 步骤1: 编辑 pyproject.toml              │
│ version = "1.0.0" → "1.1.0"            │
└─────────────────────────────────────────┘
          │
          ↓
┌─────────────────────────────────────────┐
│ 步骤2: 运行测试                         │
│ pytest tests/ -v (必须全部通过)         │
└─────────────────────────────────────────┘
          │ (SUCCESS)
          ↓
┌─────────────────────────────────────────┐
│ 步骤3: 提交版本更新                     │
│ git add pyproject.toml                  │
│ git commit -m "Bump version to 1.1.0"   │
└─────────────────────────────────────────┘
          │
          ↓
┌─────────────────────────────────────────┐
│ 步骤4: 创建Git标签                      │
│ git tag -a v1.1.0                       │
│          -m "Release version 1.1.0"     │
└─────────────────────────────────────────┘
          │
          ↓
┌─────────────────────────────────────────┐
│ 步骤5: 推送代码和标签                   │
│ git push origin development-copy        │
│ git push origin v1.1.0                  │
└─────────────────────────────────────────┘
          │
          ↓
┌─────────────────────────────────────────┐
│ 步骤6: 构建打包                         │
│ python build_package.py frozen          │
│ python -m build                         │
└─────────────────────────────────────────┘
          │
      ┌───┴───┐
      ↓       ↓
  zip文件   whl/tar.gz
      │       │
      ├─ 分发给Windows用户
      │
      └─ 上传到PyPI
```

---

## 🎯 决策树: 选择打包方式

```
                        需要打包?
                            │
                ┌───────────┴───────────┐
                ↓                       ↓
            什么用户?              什么用户?
            
    ┌───────────────────────┐   ┌────────────────────┐
    │ Windows非技术用户     │   │ Python开发者        │
    │ (需要最简单的体验)    │   │ (习惯用pip安装)     │
    └───────────────────────┘   └────────────────────┘
            │                           │
            ↓                           ↓
    ┌──────────────────┐       ┌─────────────────────┐
    │ 方式2: 冻结版本  │       │ 方式1: PyPI标准包   │
    │                  │       │                     │
    │ python build_    │       │ python -m build     │
    │ package.py       │       │ twine upload dist/* │
    │ frozen           │       │                     │
    │                  │       │ 或                  │
    │ 输出: ZIP文件    │       │ 等待GitHub Actions  │
    │ 15-20MB          │       │                     │
    │                  │       │ 输出: PyPI          │
    │ 优点:            │       │                     │
    │ - 完整独立       │       │ 优点:               │
    │ - 双击安装       │       │ - 跨平台            │
    │ - 包含所有依赖   │       │ - 规范管理          │
    └──────────────────┘       └─────────────────────┘
            │                           │
            └───────────┬───────────────┘
                        │
                分发方式不同
                    │
            ┌───────┴───────┐
            ↓               ↓
        本地分发         远程分发
        (邮件/U盘)      (网络下载)
```

---

**架构文档版本**: 1.0  
**最后更新**: 2025-11-11  
**适用范围**: development-copy 分支 (v0.9-11-g75a0085)

